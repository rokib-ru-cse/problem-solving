#include<bits/stdc++.h>
using namespace std;

void reserve(string *x,int n){
    stack<char> S;
    for (int i = 0; i < n; i++)
    {
        char z = c_str(x[i]);
        S.push();
    }
    
}
int main()
{
    string s;
    cin>>s;
    reserve(&s,s.size());
    for (int i = 0; i < s.size(); i++)
    {
        
    }
    return 0;
}