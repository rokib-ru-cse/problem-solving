#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    long long a, b, c;
    cin >> t;
    while (t--)
    {
        cin >> a >> b >> c;
        cout << (a + b + c) / 2<<endl;
    }
    return 0;
}